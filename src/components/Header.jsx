import { Container, Navbar } from "react-bootstrap";

function Header() {
    return (<Navbar className='header'>
        <Navbar.Brand>PDF-Writer</Navbar.Brand>
    </Navbar>);
}

export default Header;
